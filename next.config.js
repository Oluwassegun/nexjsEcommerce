/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["images.unsplash.com", "plus.unsplash.com" , "en.wikipedia.org"],
    },
  };
  
  module.exports = nextConfig;

